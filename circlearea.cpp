#include<stdio.h>
// area of circle = pi(r*r)
int main(){
    float pi = 3.14;
    int radius;

    printf("Enter radius of circle ");
    scanf("%d", &radius);

    printf("Area of circle is : %f",radius*radius*pi);
    return 0;
    




}